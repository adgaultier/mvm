# Build artifacts
/target
/dist

# Claude Code local state
.claude/

# Editor / local tool config
.idea
.vscode
.mcp.json

# Python (mvm-agent-mcp bridge)
__pycache__/
*.pyc
.venv/

*.oci 

# Sandbox mount artifacts (zero-byte placeholders, owned by nobody)
/.bash_profile
/.bashrc
/.gitconfig
/.gitmodules
/.profile
/.ripgreprc
/.zprofile
/.zshrc


# Integration-test runtime (daemon pid, isolated data dir)
scripts/integration/.runtime/

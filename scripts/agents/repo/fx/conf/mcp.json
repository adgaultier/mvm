{
  "mcp": {
     "mvm-agent": {
      "type": "local",
       "command": ["uvx","mvm-agent-mcp"],
     "required": true,
      "enabled": true,
      "environment": {
        "NOTIFICATION_CMD": "",
        "MVM_GUEST_TOKEN": "{env:MVM_GUEST_TOKEN}"
      }
    }
  }
}

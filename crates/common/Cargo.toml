[package]
name = "mvm-common"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
base64.workspace = true
chrono.workspace = true
dirs.workspace = true
hex.workspace = true
rand.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
uuid.workspace = true

[features]
# VM-authenticated Agent API surface (the `agent_api` module: request/response
# envelopes, `Principal`, notifications). The token *minting* plumbing
# (`generate_token`/`hash_token`) is always compiled: it is part of the guest
# env channel, gated by the `mvm` binary's feature.
agent-api = []
